import React from 'react'
export default function Props(props) {
  return (
    <div>
      <h1>My name is {props.name} password is {props.pass} i have {props.data} id Count is {props.count}</h1>

    </div>
  )
}
